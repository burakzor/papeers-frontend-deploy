# Base URL for your backend API (example: http://localhost:8080/api)
VITE_API_BASE_URL=

# Keep true while using frontend mocks. Set to false when the backend is ready.
VITE_USE_MOCKS=true

# Existing optional flag used by invitation testing flow
VITE_SHOW_INVITATION_LINKS=false
